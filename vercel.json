{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "^/assets/(.*)$", "status": 404 },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
